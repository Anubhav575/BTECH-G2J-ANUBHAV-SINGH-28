#include <stdio.h>
#include<string.h>

int main() {
    char s[20];
    int len=0,i=0;
    printf("Enter the strings: ");
    scanf("%[^\n]s",s);
    while(s[i]!='\0')
    {
        len++;
        i++;
        
    }
    printf("Length=%d",len);
    return 0;
}
