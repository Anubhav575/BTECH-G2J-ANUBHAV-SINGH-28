#include <stdio.h>
int main() {
   int sum=0,i,n;
   printf("enter the no\n");
   scanf("%d",&n);
   for (i=1;i<=n;i++)
   {
    sum=sum+i;
   }
   printf("sum of n natural no is%d",sum);
   return 0;
}
