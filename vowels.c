#include<stdio.h>
int main()
{
    char ch ;
    printf("enter the value");
    scanf("%c",&ch);
    if( ch=='a||e||i||o||u')
    {
    printf("the value is true");}
    else
    {
    printf("the value is constonants");
    }
return 0;}

