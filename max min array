# include<stdio.h>
int main()
{ 
    int a[50],n,i,max,min;
    printf("enter the no");
    scanf("%d",&n);
    for (i=0;i<n;i++)
    {
        printf("enter the a[%d]=",i);
        scanf("%d",&a[i]);}
        
    
        min=a[0];
        max=a[0];
        for (i=1;i<n;i++)
        {if ( a[i]>max)
        { 
            max=a[i];
        }
        if(a[i]<min)
        {
            min=a[i];
        }
        printf("max value=%d",max);
        printf("min value=%d",min);
        return 0;
        }}
